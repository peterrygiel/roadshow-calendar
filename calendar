<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Roadshow Calendar</title>
    <!-- Tailwind CSS for utility-first styling -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Figtree Font for a clean, modern look -->
    <link href="https://fonts.googleapis.com/css2?family=Figtree:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        /*
        Apply Figtree font and a cool, modern gradient background to the entire page.
        This provides a visually interesting backdrop for the calendar.
        */
        body {
            font-family: 'Figtree', sans-serif;
            background: linear-gradient(135deg, #004d99 0%, #0073e6 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 2rem;
        }

        /*
        Main container for the calendar. It has a clean, card-like appearance.
        Increased max-width and height to make it more prominent.
        */
        .calendar-container {
            width: 100%;
            max-width: 1400px;
            height: 90vh; /* Takes up more vertical space */
            background-color: #ffffff; /* A clean white background */
            border-radius: 1.5rem; /* More rounded corners */
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.25); /* Deeper, more defined shadow */
            overflow: hidden; /* Ensures the iframe doesn't overflow the rounded corners */
            display: flex;
            flex-direction: column;
        }

        /*
        Header for the calendar. Uses the brand's blue color and a bold font.
        */
        .calendar-header {
            background-color: #0073e6;
            color: white;
            padding: 1.5rem 2rem;
            font-size: 2rem; /* Larger font size */
            font-weight: 700; /* Bolder text */
            text-align: center;
            border-bottom: 2px solid #e2e8f0; /* Subtle line to separate from content */
        }

        /* The iframe itself, filling the remaining space in the container */
        .airtable-embed {
            width: 100%;
            height: 100%;
            border: none;
            background: transparent;
        }

        /* Responsive design for smaller screens */
        @media (max-width: 768px) {
            .calendar-header {
                font-size: 1.5rem;
                padding: 1rem 1.5rem;
            }
            .calendar-container {
                height: 95vh;
                border-radius: 1rem;
            }
        }
    </style>
</head>
<body>

<div class="calendar-container">
    <div class="calendar-header">📅 Roadshow Calendar</div>
    
    <!-- This is the iframe code for the Airtable calendar view -->
    <iframe
        class="airtable-embed"
        src="https://airtable.com/embed/appwdA9PkFSlcg5uw/shrRCpxaJ42RBY5eX"
        title="Roadshow Calendar"
        allowfullscreen
    ></iframe>
</div>

</body>
</html>

